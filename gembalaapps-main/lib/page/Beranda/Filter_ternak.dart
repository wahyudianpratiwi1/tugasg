import 'package:flutter/material.dart';

class Filterternak extends StatefulWidget {
  const Filterternak({Key? key}) : super(key: key);

  @override
  State<Filterternak> createState() => _FilterternakState();
}

class _FilterternakState extends State<Filterternak> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Filter Ternak'),
    );
  }
}
